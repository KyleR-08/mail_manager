// Manejador de autenticación.
// Se encarga de coordinar el login local del usuario y el flujo OAuth2 con Gmail
// (obtener el token, guardarlo en token/token.json y reutilizarlo en futuras sesiones).
package handlers
